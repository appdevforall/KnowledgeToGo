<?xml version="1.0" encoding="utf-8"?>
<!-- ADFA-4954: Courses confirm — the consent step. The fit banner has THREE states, not two:
     fits, does not fit, and cannot tell. The third exists because a channel Studio publishes
     without a size contributes nothing to the total, so the total can be a floor rather than
     a figure; SeedPlan.fitsIn() returns null for that, and treating it as "does not fit"
     would block downloads that would have worked. Rows are added at runtime. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingLeft="12dp"
        android:paddingRight="20dp"
        android:paddingTop="12dp">

        <ImageView
            android:id="@+id/k2go_kconf_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:src="@drawable/ic_arrow_left"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/k2go_back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="@string/k2go_zim_confirm_title"
            android:textAppearance="?attr/textAppearanceHeadlineMedium"
            android:textColor="@color/k2go_ink" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="4dp"
        android:text="@string/k2go_kolibri_confirm_sub"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="@color/k2go_muted" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <LinearLayout
                android:id="@+id/k2go_kconf_breakdown"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

            <TextView
                android:id="@+id/k2go_kconf_fits"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:padding="12dp"
                android:textAppearance="?attr/textAppearanceBodyMedium" />

            <!-- ADFA-4954: why the forward action is doing what it is doing — checking,
                 not installed, needs the box started, damaged, busy. Its own view rather
                 than reusing the fit banner: the size-versus-free-space verdict is the one
                 thing this screen exists to state, and overwriting it removed it.
                 The text is set from the dispatcher's answer, so it starts empty. -->
            <TextView
                android:id="@+id/k2go_kconf_live_note"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:padding="12dp"
                android:background="@drawable/k2go_info_bg"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="@color/k2go_info_ink"
                android:visibility="gone" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="12dp"
                android:text="@string/k2go_kolibri_takes_body"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="@color/k2go_muted" />
        </LinearLayout>
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/surface_card"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="12dp"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/k2go_kconf_confirm"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:enabled="false" />
    </LinearLayout>
</LinearLayout>
