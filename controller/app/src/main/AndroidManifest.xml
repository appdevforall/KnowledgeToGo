<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.iiab.controller">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- ADFA-4520: LocalOnlyHotspot requires location at runtime on this targetSdk -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".IIABApplication"
        android:localeConfig="@xml/locales_config"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.IIABController">

        <!-- ADFA-4533: GlitchTip (Sentry) crash reporting. DSN injected per build type via the
             ${sentryDsn} placeholder; auto-init disabled so init is consent-gated in code. -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.K2GoSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ADFA-5192: the Debian terminal. adjustResize + configChanges (the setup the terminal
             carried when it lived in MainActivity, ADFA-5024/ADFA-4987): the terminal sits in a
             BottomSheet/CoordinatorLayout and adjustResize keeps the extra-keys bar + terminal above
             the IME. singleTop lets the keep-alive notification's SINGLE_TOP intent reuse the running
             instance. Not exported: launched only in-app (redesign Settings, the terminal's own
             notification PendingIntent). -->
        <activity
            android:name=".TerminalActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".PortalActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.K2Go"/> <!-- ADFA-4886: Material 3 DayNight + app typography for the WebView chrome -->

        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

        <activity android:name=".SetupActivity" />

        <!-- ADFA-4725: new content-first UI shell (scoped Material 3 theme). -->
        <activity
            android:name=".redesign.LibraryActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.K2Go" />

        <activity
            android:name=".redesign.SetupLibraryActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.K2Go" />

        <activity
            android:name=".redesign.WizardActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.K2Go" />

        <activity
            android:name=".redesign.WizardLanguagePickerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.K2Go" />
        <activity android:name=".feedback.crash.presentation.CrashReportActivity" />
        <activity android:name=".QrActivity" />

        <receiver
            android:name=".AdbPairingReceiver"
            android:exported="false" />

        <service
            android:name=".WatchdogService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <service
            android:name=".install.presentation.InstallService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <service
            android:name=".deepop.DeepOpService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <service
            android:name=".redesign.CloneShareService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <service
            android:name=".redesign.ZimDownloadService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <service
            android:name=".redesign.BooksDownloadService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- ADFA-4954: seeds Kolibri channels through the in-server REST job engine. Same shape as
             the ZIM and Books services above: not exported, and specialUse because the work runs
             inside the co-located proot rather than being a stock data transfer. -->
        <service
            android:name=".kolibri.presentation.KolibriSeedService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <activity
            android:name=".redesign.SetupProgressActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.K2Go" />

        <service
            android:name=".TerminalSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Lets AppCompat store + reapply the per-app UI language (ADFA-4304). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- ADFA-4466 Phase 1: privacy hardening. Disable Advertising ID / SSAID
             collection and ad personalization (K2Go may serve minors; operational
             analytics only). -->
        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_ssaid_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_default_allow_ad_personalization_signals" android:value="false" />
    </application>
</manifest>
